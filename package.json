{
  "name": "akonwi.github.io",
  "version": "0.1.0",
  "packageManager": "bun@1.2.16",
  "private": true,
  "scripts": {
    "build": "ard build site.ard --target go --out site-gen && ./site-gen",
    "test": "ard test site.ard",
    "clean": "rm -rf dist site-gen ard-out"
  }
}
